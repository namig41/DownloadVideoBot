# Download Video Bot

Telegram бот для скачивания коротких видео из Instagram, TikTok и YouTube Shorts.

## Установка и настройка

### Требования

- Python 3.10+
- [uv](https://github.com/astral-sh/uv) - быстрый менеджер пакетов Python

### Установка uv

```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Или через pip
pip install uv
```

### Установка зависимостей

```bash
# Установка всех зависимостей через uv
make install
# или
uv sync
```

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Token
VIDEO_DOWNLOADER_TOKEN=your_bot_token_here

# Telegram Admin IDs (через запятую)
TELEGRAM_ADMINS_ID=123456789,987654321

# PostgreSQL (опционально, если не указано, используется SQLite)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=video_downloader

# Worker
WORKER_CONCURRENCY=4
```

### Применение миграций

```bash
make upgrade
# или
uv run alembic upgrade head
```

## Запуск

### Локальный запуск

```bash
# Запуск бота
make bot
# или
uv run python src/bot/main.py
```

### Запуск через Docker

```bash
# Сборка образов
make docker-build
# или
docker-compose build

# Запуск всех сервисов (бот + PostgreSQL)
make docker-up
# или
docker-compose up -d

# Просмотр логов
make docker-logs
# или
docker-compose logs -f bot

# Остановка контейнеров
make docker-down
# или
docker-compose down
```

## Команды Makefile

### Основные команды
- `make install` - Установить зависимости через uv
- `make sync` - Синхронизировать зависимости (uv sync)
- `make migrate` - Создать новую миграцию
- `make upgrade` - Применить миграции
- `make downgrade` - Откатить последнюю миграцию
- `make bot` - Запустить бота локально

### Docker команды
- `make docker-build` - Собрать Docker образ
- `make docker-up` - Запустить контейнеры
- `make docker-down` - Остановить контейнеры
- `make docker-logs` - Показать логи контейнеров
- `make docker-shell` - Открыть shell в контейнере бота

## Использование uv

### Основные команды

```bash
# Установка зависимостей
uv sync

# Добавление новой зависимости
uv add package-name

# Добавление dev зависимости
uv add --dev package-name

# Запуск команды в виртуальном окружении
uv run python script.py

# Запуск команды напрямую
uv run alembic upgrade head
```

## Структура проекта

```
src/
├── bot/              # Логика бота
│   ├── handlers/     # Обработчики команд и сообщений
│   ├── bot.py        # Создание диспетчера
│   └── main.py       # Точка входа
├── config/           # Конфигурация
├── database/         # Модели и работа с БД
└── services/         # Бизнес-логика
    ├── user_service.py
    └── video_downloader.py
```

## Особенности

- ✨ Бесплатно и без ограничений
- 🎥 Поддержка Instagram, TikTok, YouTube Shorts
- 📊 Статистика пользователей
- 📢 Массовая рассылка для администраторов
- 🗄️ Поддержка PostgreSQL и SQLite

